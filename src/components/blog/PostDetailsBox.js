import styled from "@emotion/styled";

import Caption from "../Caption";

const PostDetailsBox = styled(Caption)``;

PostDetailsBox.defaultProps = {
  fontSize: 1,
  fontWeight:2,
  fontFamily: 'special',
  mb: 3,
};

export default PostDetailsBox;
