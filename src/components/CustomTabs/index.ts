export { default as CustomTabs } from './CustomTabs';
export { default as CustomTabList } from './CustomTabList';
export { default as CustomTab } from './CustomTab';
export { default as CustomTabPanel } from './CustomTabPanel';

// Just for consistency with the rest of the custom tabs components composition
export { TabPanels as CustomTabPanels } from '@chakra-ui/react';
